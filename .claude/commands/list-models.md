List all available Stable Diffusion models in ComfyUI/models/ directory.

Show:
1. Checkpoints in `ComfyUI/models/checkpoints/`
2. LoRAs in `ComfyUI/models/loras/`
3. VAE in `ComfyUI/models/vae/`
4. ControlNet in `ComfyUI/models/controlnet/`
5. Inpainting models in `ComfyUI/models/inpaint/`
6. Total disk usage per category
